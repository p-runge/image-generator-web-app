# Python API URL (without trailing slash)
PYTHON_API_URL=http://localhost:8765

# Shared secret — must match API_SECRET in the Python app
# Leave empty to disable auth (local dev only)
API_SECRET=replace-with-a-long-random-string
